# Comma-separated list of databases to auto-create on first Postgres startup.
# Add one entry per service, e.g. orders_service,users_service
POSTGRES_MULTIPLE_DATABASES=todos_service,auth_service

# Signs auth sessions and JWTs. Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
