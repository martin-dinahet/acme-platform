# Public URL of the API gateway, as the browser sees it. Inlined at build time.
VITE_API_URL="http://localhost:3000"

# Public URL of the standalone login app. Signed-out users are redirected here. Inlined at build time.
VITE_LOGIN_URL="http://localhost:5174"
