# Required — no default, service will refuse to start without it.
PORT=3001

# Points at the "todos_service" database on the shared local Postgres container.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/todos_service"
